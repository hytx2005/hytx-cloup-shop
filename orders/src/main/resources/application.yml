server:
  port: 8081

spring:
  application:
    name: orders-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public


management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.example.orders: DEBUG
    org.apache.dubbo: INFO
    com.alibaba.nacos: WARN