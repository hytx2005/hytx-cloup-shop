server:
  port: 8082

spring:
  application:
    name: users-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.example.users: DEBUG
    org.apache.dubbo: INFO
    com.alibaba.nacos: WARN